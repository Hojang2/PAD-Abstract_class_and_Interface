package school.absInt;

public interface Repair {
    public Template repairProduct(); // return the same object but with
    public boolean checkProduct(); //Check if product need to repair or replace
}
